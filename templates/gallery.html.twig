{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            {% for image in images %}
                {% include 'image_thumbnail.html.twig' %}
            {% endfor %}
    </div>
</div>
{% endblock %}
