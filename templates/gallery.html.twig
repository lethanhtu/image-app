{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">


        {% set num = 0 %}
        <div class="row">
            {% for image in images %}
                {% include 'image_thumbnail.html.twig' %}
                {% set num = num + 1 %}
                {% if num > 3 %}
                </div>
                <div class="row">
                    {% set num = 0 %}
                {% endif %}
            {% endfor %}
        </div>
    {% endblock %}
